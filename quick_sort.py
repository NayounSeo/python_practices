import math

def quick_sort (l, start, end):
    if (start < to):
      mark = quick_partition (l, start, end)
      quick_sort (l, start, mark - 1)
      quick_sort (l, mark, end)

def quick_partition (l, start, end):
    end_val = l[end]
    temp = 0

    for 

if __name__ == "__main__":
  l = [[3, 2], [5, 2], [4, 2], [6, 2]]
  print()